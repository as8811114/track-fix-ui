# Architecture

## Repository Responsibility

`track-fix-ui` contains the frontend application only.

The frontend and backend are maintained as separate projects.

## Current Phase

Backend services are not implemented yet.

The frontend uses mock data and a data-access abstraction so mock implementations can later be replaced by real backend APIs without rewriting UI components.